return {
    {
        "dcampos/nvim-snippy",
        config = function()
            require('snippy').setup({
                mappings = {
                    is = {
                        ['<C-J>'] = 'expand_or_advance',
                        ['<C-K>'] = 'previous',
                    },
                    nx = {
                        ['<leader>x'] = 'cut_text',
                    },
                },
            })
        end,
    }
}
